# PHPStorm
.idea

# OSX
.DS_Store

# Composer
vendor/
composer.lock

# npm (jscpd, installed by the composer post-install-cmd/post-update-cmd hooks)
node_modules/
package.json
package-lock.json

# Build stuff (incl. all dev-tool caches under .build/cache/)
.build/
